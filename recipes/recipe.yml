# image will be published to ghcr.io/<user>/<name>
name: fcos-server 
# description will be included in the image's metadata
description: Custom FCOS Server

# the base image to build on top of (FROM) and the version tag to use
# base-image: ghcr.io/ublue-os/ucore
base-image: quay.io/fedora/fedora-coreos 
image-version: stable # latest is also supported if you want new updates ASAP

modules:
  - type: dnf
    install:
      packages:
        - vim
        - btop
        - zsh
        - atuin
        - podlet
        - util-linux-script
        - rpmconf
# The following install packages is here until ublue gets their images fixed
        - qemu-guest-agent
    remove:
      packages:
        - nano
  - type: dnf
    repos:
      copr:
        - atim/starship
    install:
      packages:
        - starship
  - type: systemd
# Disabled Zincadi on FcOS until ublue works again
    system:
      disabled:
        - zincati.service
  - type: signing

